apiVersion: v1
kind: Service
metadata:
  name: dev-deployment-service
spec:
  type: LoadBalancer
  selector:
    app: dev-app  # This must match your deployment's labels
  ports:
    - port: 8080
      targetPort: 8080  # Matches containerPort in your deployment
      protocol: TCP